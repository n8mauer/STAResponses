# Opening Statement

---

Thank you for the opportunity. I'm a research engineer who builds multi-agent systems, agent evaluation environments, and LLM training pipelines — from RL reward functions to production infrastructure. I don't just design systems on whiteboards; I ship them.

I've built OpenEnv-compliant agent environments with MCTS planning and GRPO RL training, implemented PPO-trained reinforcement learning agents for model safety, fine-tuned LLMs using QLoRA, and deployed production applications with Claude API and GPT-4o — all backed by infrastructure I authored in Terraform and CloudFormation across 17 AWS services.

To give you a sense of range and depth:

I built **Nexus**, a multi-agent compute cluster negotiation environment at the OpenEnv Hackathon SF (Meta/HuggingFace, March 2026). 2-8 agents trade scarce GPU/CPU/memory resources, form coalitions via Mixture-of-Experts voting, and complete jobs under deadlines — all under partial observability rendered as natural language. The system implements Monte Carlo Tree Search (UCB1) for strategic negotiation planning, CART behavioral probes for real-time anomaly and collusion detection, Claude tool-use integration with experience replay buffers, and a 7-phase round loop with market physics (price impact, commission, Sharpe ratio, Gini fairness). Seven agent types from random baselines to MCTS strategists to Claude, validated across 795 lines of pytest. 4,255 lines of Python.

I built **HCM:21**, a long-horizon agent evaluation environment where AI agents manage 200-500 employees over 6 quarters (150-200+ steps) with sparse delayed rewards — Q1 decisions show ROI in Q3+. A 4-phase state machine (Scan > Plan > Produce > Control) gates 16 action types, with Cobb-Douglas production functions, logistic flight risk modeling, and 8 stochastic event types cascading across quarters. The 6-component scoring system is calibrated across baselines (Random 0.15-0.25, Heuristic 0.50-0.65, Strong LLM 0.70+). Agents train via GRPO (TRL + Unsloth) on Qwen3-0.6B, and the Claude-based agent uses phase-aware prompting with quarterly LLM-generated memory compression for 100K+ token histories. Deployed on HuggingFace Spaces. 2,478 lines + 400 lines of tests.

I architected a **backdoor detection system** for 671-billion parameter LLMs — probing residual stream activations with per-layer linear classifiers, unsupervised CCS labeling, and custom CART ensembles, hardened by 4 adversarial PPO agents coordinating via a solver loop harness with cross-strategy data flow and convergence detection. That codebase has 10,400+ lines, 205 tests, strict mypy, and a 6-job CI/CD pipeline because research code that can't be trusted produces results that can't be trusted. The probe reliability scoring caught false confidence before wasting compute on the 671B model.

A 3-person team coded and sold a **federal code refactoring pipeline** to the Federal government — processing 2M+ lines of legacy mainframe code (COBOL/Assembly) through a 200K context window into production Python, reducing transformation cost by 1000x. This required four agent-pattern architectures: a Step Functions sense-plan-act agent harness for autonomous training lifecycle management with checkpoint recovery and spot instance adaptation; intelligent document chunking agents that extract metadata, detect section boundaries, split with context-preserving overlap, and merge into coherent Python modules; a curriculum-style multi-run seed strategy (5 seeds x 10K = 50K examples) for data mix optimization; and an orchestration layer that dynamically decides chunk boundaries based on document structure and produces globally coherent output from locally processed pieces. Fine-tuned CodeLlama-7B via QLoRA achieving 84/100 output quality, 85-90% business logic accuracy, and 6,435 lines generated.

I designed and shipped a **production AI-integrated platform** serving live users — React 18, Express 5, PostgreSQL, GPT-4o wired into 7 distinct features with hash-keyed response caching, multi-provider OAuth, and presigned S3 uploads. It's live at cookbookclub.vip.

And I built a **RAG-powered study assistant** with a 10-stage processing pipeline — screen capture, OCR, semantic chunking, ChromaDB vector retrieval, context merging, and Claude API generation — latency-budgeted at under 4 seconds, with an 8-agent dependency graph and typed interface contracts so components can be swapped without touching the rest of the system.

I also built **game-playing agents** with minimax and alpha-beta pruning across 5 variants including 3-player Hidden Multiplayer with partial observability, and a **market simulation engine** with Sharpe ratio benchmarking and a pytest-parametrized grading framework with 12 test cases at 0.1% tolerance.

What ties all of this together is how I think about agent systems: structured state machines with phase-gated action spaces, quantitative benchmarks calibrated across agent baselines, memory management that scales beyond context limits, and a bias toward shipping working systems over producing slide decks. I evaluate design choices the way agent research demands — not "what's newest" but "what produces reliable, measurable improvements in agent performance."

I'm comfortable at every layer — GRPO reward function design, PyTorch model internals, cloud infrastructure provisioning, API design, frontend delivery — and I'm effective at translating between them for research and engineering audiences alike.
