Nathaniel (Nate) Mauer
n8mauer@gmail.com
C: 703-966-1007

07/07/2025

Anthropic
San Francisco, CA

Re: Research Engineer, Agents

Dear Hiring Team,

I want to help make Claude a better agent. I've spent the last several years building the exact systems your JD describes — multi-agent coordination, agent harness design, quantitative benchmarks, automated evaluation, and LLM finetuning — and I've hit the problems that make this work hard. Partial cache hit ambiguity under multi-agent interleaving creates unpredictable latency variance across turns. Dependency-aware DAG orchestration breaks down when agent outputs invalidate upstream assumptions. Context compression forces tradeoffs between information density and retrieval fidelity that no single heuristic resolves. These aren't theoretical concerns for me; they're bugs I've debugged in my own systems, and the reason I want to work on them at the frontier.

My most recent work is the strongest signal. At the OpenEnv Hackathon SF (Meta/HuggingFace, March 2026), I built Nexus — a multi-agent compute cluster negotiation environment where 2-8 agents trade scarce GPU/CPU/memory resources, form coalitions via Mixture-of-Experts voting, and complete jobs under deadlines with partial observability. I implemented Monte Carlo Tree Search with UCB1 selection for strategic negotiation planning under hidden information, CART behavioral probes (RandomForest, 20 trees, 8-dim feature vectors) for real-time anomaly and collusion detection, and a Claude tool-use integration with experience replay buffers injecting best-N and recent-N memories into agent context. The environment runs a 7-phase round loop — Event, Observe, Negotiate, Action, Execute, Score, Oversight — with market physics (price impact, commission), Sharpe ratio scoring, and Gini coefficient fairness metrics. Seven agent types from random baselines to MCTS strategists to Claude tool-use agents, all validated across 795 lines of pytest. This is agent harness design, multi-agent coordination, and automated evaluation in a single system.

I also built HCM:21, an OpenEnv-compliant long-horizon agent evaluation environment where AI agents manage 200-500 employees over 6 quarters (150-200+ steps) with sparse delayed rewards — hiring decisions in Q1 show ROI in Q3+. The environment enforces structured decision-making through a 4-phase state machine (Scan, Plan, Produce, Control) gating 16 action types, with Cobb-Douglas production functions, logistic flight risk modeling, and 8 stochastic event types that cascade across quarters. I designed a 6-component scoring system calibrated across agent baselines (Random 0.15-0.25, Heuristic 0.50-0.65, Strong LLM 0.70+) and trained agents via GRPO on Qwen3-0.6B with a custom reward function that executes full episodes as RL signal. The Claude-based agent uses phase-aware prompting with quarterly LLM-generated memory compression to manage 100K+ token episode histories — directly addressing the context management challenges your team works on.

These environments build on deeper systems work. My residualstreambackdoor project implements 4 PPO agents in PyTorch that coordinate via a solver loop harness to detect hidden backdoor triggers in a 671B-parameter LLM — with cross-strategy data flow between residual stream probing and behavioral analysis, adversarial hardening cycles, and a PPOMonitoringPolicy that acts as an autonomous quality gate with accept/reject/escalate actions. That codebase has 10,400+ lines, 205 tests, strict mypy, and a 6-job CI/CD pipeline. The probe reliability scoring — combining sample size, train-test gap, layer position, and PCA separability into a composite metric — caught false confidence before wasting compute on the full 671B model.

My game-playing agent framework implements minimax and alpha-beta pruning across 5 variants including 3-player Hidden Multiplayer with partial observability — the same polymorphic dispatch problem your team solves when building a single agent harness that adapts strategy to task complexity. My market simulation includes a pytest-parametrized grading framework with 12 test cases and 0.1% tolerance assertions — the evaluation pattern that scales to rigorous agent benchmarking.

I build production LLM systems too. My RAG study assistant runs a 10-stage pipeline with an 8-agent dependency graph, context compression, and Claude API generation in under 4 seconds. My production platform at cookbookclub.vip integrates GPT-4o at 7 touchpoints with hash-keyed response caching. For HiddenSignalTest, a 3-person team coded and sold a code refactoring pipeline to the Federal government — processing 2M+ lines of legacy mainframe code (COBOL/Assembly) through a 200K context window into production Python. This required solving four agent-architecture problems: a Step Functions sense-plan-act pipeline for autonomous training lifecycle management with checkpoint recovery and spot instance adaptation; intelligent document chunking agents that extract metadata, detect section boundaries, split with context-preserving overlap, cache per-chunk results (MD5-keyed), and merge into coherent Python modules; a curriculum-style multi-run seed strategy (5 seeds x 10K = 50K examples) for data mix optimization across 8 weighted domains; and an orchestration layer chaining converters that dynamically decide chunk boundaries based on document structure and produce globally coherent output from locally processed pieces. Fine-tuned CodeLlama-7B via QLoRA achieving 84/100 output quality and 85-90% accuracy — confirming that agent harness design and data mix optimization matter more than raw volume.

What draws me to Anthropic specifically is the intersection of agent capability and safety. My backdoor detection work exists because I believe deployed models need governance — the same conviction that drives Anthropic's mission. I want to make Claude more capable as an agent while ensuring that capability remains interpretable and steerable. I'm pursuing my M.S. in Computer Science (Machine Learning) at Georgia Tech, I hold a Top Secret clearance, and I'm comfortable at every layer from GRPO reward function design to production infrastructure.

I would welcome the opportunity to discuss how my experience building multi-agent environments, evaluation frameworks, and agent harnesses maps to what the Agents team is working on.

Sincerely,
Nathaniel (Nate) Mauer
